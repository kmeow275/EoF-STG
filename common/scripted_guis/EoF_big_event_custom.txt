scripted_gui = {	
	#island war
	EoF_big_event_island_war_custom = { ### VERY IMPORTANT MAKE SURE THIS USES THE SAME NAME AS #IN THE WW_BigEvent_custom.gui FILE OR YOU'RE GONNA GET A CRASH
		context_type = player_context
		window_name = "EoF_big_event_island_war_custom_window"

		visible = {
			has_country_flag = EoF_big_event_custom_island_war ### this global flag triggers the #superevent
		}
	}

	EoF_big_event_frame_island_war_custom = { ### VERY IMPORTANT MAKE SURE THIS USES THE SAME #NAME AS IN THE WW_BigEvent_custom.gui FILE OR YOU'RE GONNA GET A CRASH
		context_type = player_context
		window_name = "EoF_big_event_frame_island_war_custom"

		visible = {
			has_country_flag = EoF_big_event_custom_island_war
		}

		effects = {
			Option_click = {
				clr_country_flag = EoF_big_event_custom_island_war ### this disables the country flag #that fires the superevent (so the superevent actually goes away)
			}
		}
	}

	#great war
	EoF_big_event_great_war_custom = { ### VERY IMPORTANT MAKE SURE THIS USES THE SAME NAME AS #IN THE WW_BigEvent_custom.gui FILE OR YOU'RE GONNA GET A CRASH
		context_type = player_context
		window_name = "EoF_big_event_great_war_custom_window"

		visible = {
			has_country_flag = EoF_big_event_custom_great_war ### this global flag triggers the #superevent
		}
	}

	EoF_big_event_frame_great_war_custom = { ### VERY IMPORTANT MAKE SURE THIS USES THE SAME #NAME AS IN THE WW_BigEvent_custom.gui FILE OR YOU'RE GONNA GET A CRASH
		context_type = player_context
		window_name = "EoF_big_event_frame_great_war_custom"

		visible = {
			has_country_flag = EoF_big_event_custom_great_war
		}

		effects = {
			Option_click = {
				clr_country_flag = EoF_big_event_custom_great_war ### this disables the country flag #that fires the superevent (so the superevent actually goes away)
			}
		}
	}

	#sunset
	EoF_big_event_sunset_custom = { ### VERY IMPORTANT MAKE SURE THIS USES THE SAME NAME AS #IN THE WW_BigEvent_custom.gui FILE OR YOU'RE GONNA GET A CRASH
		context_type = player_context
		window_name = "EoF_big_event_sunset_custom_window"

		visible = {
			has_country_flag = EoF_big_event_custom_sunset ### this global flag triggers the #superevent
		}
	}

	EoF_big_event_frame_sunset_custom = { ### VERY IMPORTANT MAKE SURE THIS USES THE SAME #NAME AS IN THE WW_BigEvent_custom.gui FILE OR YOU'RE GONNA GET A CRASH
		context_type = player_context
		window_name = "EoF_big_event_frame_sunset_custom"

		visible = {
			has_country_flag = EoF_big_event_custom_sunset
		}

		effects = {
			Option_click = {
				clr_country_flag = EoF_big_event_custom_sunset ### this disables the country flag #that fires the superevent (so the superevent actually goes away)
			}
		}
	}
}